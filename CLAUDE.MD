# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Deploy

Editar `puntoflix_firebase.html` dispara automáticamente dos hooks en `~/.claude/settings.json`:
1. Copia y despliega a Firebase (async)
2. `git add` + `git commit` + `git push` a GitHub (async)

Si falla el hook de Firebase:
```powershell
Copy-Item "C:\Users\jozz1\Downloads\PROYECTOS CLAUDE\GESTOR DE USUARIOS\puntoflix_firebase.html" C:\puntoflix\public\index.html -Force
Set-Location C:\puntoflix; firebase deploy --only hosting
```

Si falla el hook de GitHub:
```powershell
Set-Location "C:\Users\jozz1\Downloads\PROYECTOS CLAUDE\GESTOR DE USUARIOS"
git add puntoflix_firebase.html; git commit -m "manual sync"; git push
```

El repo también versiona `index.html` (redirect a `puntoflix_firebase.html`), `CNAME` (app.puntoflix.com), `sw.js` (service worker network-first, copia de `C:\puntoflix\public\sw.js`) y `404.html`.

Live: https://gestor-puntoflix.web.app · Firebase: `gestor-puntoflix` · GitHub: `jozz1397-cloud/gestor-puntoflix` (privado)

## Arquitectura

Un solo archivo HTML (~2800 líneas). Sin build. HTML + CSS + JS inline.

**Estado global:** objeto `S` (~línea 299). `render()` reconstruye el DOM completo. `renderCont()` solo rerenderiza `.cont` (más ligero).

**Unir clientes duplicados ya existentes (`detectarClientesDuplicados`/`abrirUnirDuplicados`/`renderUnirDupMod`/`confirmarUnirDuplicados`, estado `S.dupMod`):** herramienta de limpieza en **Ajustes → Datos → "🔗 Clientes Duplicados"** (muestra el nº detectado). `detectarClientesDuplicados()` agrupa: (1) por **teléfono** (≥7 dígitos iguales) → seguro; (2) por **nombre** `norm()` entre los que no cayeron en (1), y **descarta** el grupo si hay 2+ teléfonos distintos entre los miembros (probables homónimos). Devuelve `[{tipo:'tel'|'nombre', principal, otros[]}]`; **principal** = el de más suscripciones (desempata: con teléfono, luego el primero). Los grupos viven en la variable de módulo **`_dupGroups`** (no se persiste); selección en `S.dupSel` (Set de índices, todos por defecto). El modal lista cada grupo (los "nombre" con badge ⚠️ "revisa"). `confirmarUnirDuplicados` mueve las subs de cada `otro` al `principal` **clonándolas** (`{...s}`, para no compartir referencia con la copia de papelera), completa teléfono/notas, reactiva, **reasigna los pagos** (`p.clientId`→principal) y manda el duplicado vacío a **`S.papelera`** (con sus subs originales + `_unidoA`, recuperable, auto-purga 30 días). Enganchado en `renderApp`, `closeAll` y el guard `editando` del sync (para que un sync no reemplace `S.clients` con el modal abierto y deje `_dupGroups` colgando). Solución para la base YA duplicada; el `submitCMod` evita crear nuevos.

**Anti-duplicados al crear cliente (`submitCMod`, modo `add`):** antes de crear, se busca un cliente EXISTENTE y, si lo hay, se **ANEXA la nueva suscripción a ese cliente** en vez de duplicar (variable local `mergeTarget`). Detección: (1) **teléfono** normalizado a dígitos, ≥7 dígitos iguales → match fuerte, anexa **sin preguntar**; (2) si no, **nombre** vía `norm()` (sin tildes/mayúsculas: "Solórzano"=="solorzano") → match débil, pide `confirm` (Aceptar=anexar / Cancelar=crear aparte, por si es homónimo). Al anexar: `newClientId=mergeTarget.id` (para que el pago inicial y el modal de bienvenida apunten al cliente existente), `mergeTarget.subs=mergeTarget.subs.concat(subs)`, completa `phone` si faltaba, reactiva si estaba Suspendido/Cancelado, y concatena `notes`. El modal de bienvenida muestra "➕ Suscripción Añadida" (flag `S.welcomeMerged`, reseteado en `cerrarWelcome`) y el toast dice "Suscripción añadida a X (sin duplicar)". Esto es la solución de raíz al problema de registros duplicados de la misma persona; complementa la agrupación por teléfono en recordatorios. La **importación por Excel (`confirmImport`)** usa la misma lógica: agrupa las filas por `ph:digits`(≥7) o `nm:norm(nombre)`, y contra los clientes existentes une por teléfono (si ambos tienen, deben coincidir) o por nombre normalizado — así no duplica al importar. Los **Combos** (`submitCombo`) todavía NO deduplican (tienen su propio modo 'existing' manual). [[prefer-grouping-over-editing-data]]

**Agrupación de recordatorios por MISMO teléfono (une clientes duplicados):** helper **`_phoneKey(c)`** = teléfono a dígitos. El dashboard "Vencimientos Próximos" (`renderDash`), el modal "Recordatorios del día" (`getListaEnvio`) **y "Seguimiento de cobros" (`getSeguimiento`)** agrupan por teléfono. Los dos primeros por **`(phone?'ph:'+digits:'id:'+cId) + '|' + endDate`**; `getSeguimiento` por **`ph:'+digits` o `id:'+cId`** (sin fecha, junta todas las vencidas de la persona). `getSeguimiento` guarda `items:[{c,s}]` y `renderSeguirMod` marca con **`marcarSeguimientoItems('cId:sId,...')`** (dueño real por sub); "contactado" = `g.items.every(i=>segEnviado(...))`. Así **dos registros distintos de la misma persona** (p.ej. "Felix Sol**ó**rzano" y "Felix Sol**o**rzano", mismo teléfono) se unifican en una sola fila/mensaje aunque sean apps diferentes. Decisión del dueño: **agrupar por teléfono, NO fusionar/editar los registros** (ver [[prefer-grouping-over-editing-data]]). Cada grupo guarda **`items:[{c,s}]`** (el dueño REAL de cada sub, no solo `subs[]`), porque los marcadores de enviado se llavean por `cId_sId` y cada sub puede pertenecer a un registro distinto. Marcado con **`marcarEnviadoItems('cId:sId,cId:sId')`** (variante de `marcarEnviadoGrupo` que respeta el dueño por sub); "enviado" del grupo = `g.items.every(i=>envEnviado(i.c.id,i.s.id,i.s.endDate))`. El nombre/avatar del grupo usa el primer dueño (`g.c`); el mensaje combinado es `genWACombinado(g.c, subs)`.

**Win-back (RETIRADO por decisión del dueño):** existió brevemente una función "😴 Recuperar clientes" (lista de inactivos 30+ días/suspendidos/cancelados con mensaje de WhatsApp para invitarlos a volver). El dueño pidió quitarla — **no reimplementar ni volver a sugerirla**. Nota: documentos en Firestore pueden conservar un campo `waWinback` huérfano de esa época; `aplicarDatosFirestore` simplemente lo ignora (inofensivo).

**Método de pago en los cobros (RETIRADO por decisión del dueño):** existió brevemente un selector de método (Efectivo/Transferencia/etc.) en los 4 modales de cobro que guardaba `p.metodo` + un bloque "Ingresos por Método" en Reportes. El dueño pidió eliminarlo — **no reimplementar ni volver a sugerirlo**. Algunos pagos de esa época pueden conservar un campo `metodo` huérfano (inofensivo, nada lo lee). Nota: `s.paymentMethod` en las subs y el selector `#spm` del modal de suscripción son ANTERIORES y siguen existiendo (no se tocaron).

**Tarjeta "Por Cobrar" del dashboard:** `stats()` expone **`porCobrar`** = suma de `precioMostrar` de TODAS las subs cobrables (`gst` 'exp' o 'ven') de clientes no cancelados — consistente con el contador `exp+ven` de la tarjeta (antes mostraba `st.deuda`, que solo suma vencidos, y salía "$0 pendiente" con clientes por vencer). La tarjeta muestra "$X pendiente · $Y ya vencido" (el desglose vencido solo si `st.ven>0`). `deuda`/`cliDeuda` NO cambiaron (siguen siendo solo vencidos; los usan el badge y otras vistas).

**Combos editables (`getCombos()`/`_combosEdit()`/`saveComboPrecio`/`delComboDef`/`addComboDef`, estado persistido `S.combos`):** UI en **Ajustes → Negocio → "🎁 Combos"**: cambiar precio (💾), quitar (🗑️) y crear combo nuevo de **2 a 6 apps**: fila `#cbAppsRow` con 2 selects fijos (`.cbAppSel`) + botón "➕ Otra app" (`addComboApp()`, añade selects extra por **DOM directo** con ✕ para quitarlos — un `renderCont()` borraría lo ya elegido, por eso no re-renderiza). `addComboDef()` lee todos los `.cbAppSel` de la fila, valida ≥2 apps, sin repetidas por `platGroup`, precio > 0 y que no exista ya un combo con ese mismo set; el nombre es `apps.join(' & ')`. Todo el flujo aguas abajo (`submitCombo`, `comboDe`, `genWACombo`, modal de venta, welcome) recorre `combo.apps` genéricamente, así que los combos de 3+ apps funcionan sin código especial. `S.combos` arranca `null` = usar los defaults `COMBOS`; al primer cambio `_combosEdit()` lo materializa como copia profunda y desde ahí manda (un array VACÍO es válido = sin combos; `openCombo` avisa y no abre). **Usar SIEMPRE `getCombos()`, nunca `COMBOS` directo** (migrados: `comboDe`, `renderComboMod`, `submitCombo`). Persistido como JSON string en `sv()` (`''` cuando es null → `aplicarDatosFirestore` solo lo aplica si parsea a array), en `_guardarEmergencia` y en el restore local de `ld()`. Quitar un combo NO afecta a clientes que ya lo tienen (`comboDe` dejará de detectarlo como combo en mensajes/totales — precio vuelve a la suma de sueltos).

**Saneo de texto de entrada (`sanTxt(v)`):** quita `<` y `>` (romperían el HTML al renderizar, no hay escape global en la app) y hace trim. Aplicado al guardar: nombre/notas de cliente (`submitCMod`), edición combinada (`scn`/`scnotes` en `submitSMod`), proveedor (`submitProvMod` nombre/notas), nombre en combo (`submitCombo`) y nombre/notas del import Excel (`confirmImport`). NO se aplica a contraseñas ni correos (podrían llevar símbolos legítimos) ni retroactivamente a datos ya guardados.

**Auditoría de datos (`auditarDatos()`/`abrirAuditoria()`, "🩺 Salud de los Datos"):** sección en **Ajustes → Datos** (entre "Clientes Duplicados" y "Resumen del Sistema") con contador de hallazgos y botón que abre el modal `#audMod` (DOM directo, sin clic-fuera). `auditarDatos()` SOLO LEE (nunca corrige) y devuelve categorías `{sev:'R'|'Y'|'I', titulo, detalle, items:[{label,det,cId,sId,caId}]}`: 🔴 sin fecha de fin, fin<inicio, cuenta maestra rota (masterAccountId sin cuenta viva; distingue si está en papelera), cuenta sobrevendida (profInUse>límite); 🟡 sin precio (`precioMostrar<=0`), sin plataforma, perfil repetido en la misma cuenta (solo Activos, ignora `cuentaCompleta`), correo/clave desincronizados vs su cuenta maestra, duplicados (reusa `detectarClientesDuplicados`, botón 🔗 Unir → `abrirUnirDuplicados`); ⚪ sin teléfono, sin cuenta asignada, sin ninguna suscripción. Cancelados excluidos de todo. Cada item lleva botón "Ir →" (`irACuenta`/`irASuscripcion`/`irACliente`, cierran el modal). Definidas justo antes de `detectarClientesDuplicados`.

**Eliminar pago mal registrado (`delPago(pId)`):** botón 🗑️ en cada fila del historial por cliente (`renderHistorialMod`), en la tabla "📋 Cobros del Mes" de Reportes y en la lista "Gastos registrados este mes" (solo si `p.id` existe; pagos legacy sin id no muestran botón). Con `confirm`; borra SOLO el registro de `S.pagos` (corrige "Cobrado mes"/reportes) — **no toca** fechas de vencimiento ni subs. `render()` + `sv()`.

**Recibo de pago en PDF (`reciboPDF(pagoId)`):** botón 🧾 junto al 🗑️ en el historial por cliente y en "Cobros del Mes" (solo pagos con `id` y NO `esCosto`). Genera con jsPDF (`loadJsPDF` on-demand) un recibo media-carta con banda navy+lima de la marca, N.º (últimos 8 del id), cliente, servicio, "activo hasta", detalle y monto destacado. Sin emojis (helvetica estándar). Se descarga para adjuntarlo en WhatsApp manualmente.

**Publicar catálogo en la web (`publicarCatalogoWeb()`):** botón "🏪 Publicar en mi página" en Ajustes → Negocio → Catálogo. Con `confirm` (muestra el resumen), escribe `{precios:JSON, combos:JSON, biz, updatedAt}` en el doc **`publico/catalogo`** de Firestore — ÚNICA escritura que NO pasa por `sv()` (es otro documento, no el de datos). La landing (`../PUNTOFLIX LANDING/index.html`) lo lee por REST sin login y actualiza sus precios sola (ver su CLAUDE.md). **Reglas de Firestore** desplegadas desde `C:\puntoflix\firestore.rules` (`firebase deploy --only firestore:rules`): `usuarios/{uid}` solo dueño autenticado, `publico/*` lectura pública + **escritura SOLO del uid del dueño** (`OWNER_UID`, ver sección Multi-usuario), todo lo demás bloqueado. Si se cambian las reglas, mantener esas tres cláusulas.

**Lista de precios para WhatsApp (`copiarListaPrecios()`):** botón "📣 Copiar lista de precios" en Ajustes → Negocio → Catálogo. Arma un mensaje con `S.biz` + plataformas del catálogo con precio > 0 (`PLATS` sin 'Otro', emoji `pi(p)`) + combos de `getCombos()` con precio > 0, y lo copia con `cp()`. Si no hay nada con precio, toast de aviso. No persiste nada; se genera al vuelo.

**Gastos manuales del negocio (`registrarGasto()`/`confirmarGasto()`, modal DOM directo `#gastoMod`):** botón "➕ Registrar gasto" en la cabecera de "💹 Costo vs Ganancia" (Reportes). Guarda en `S.pagos` un pago negativo `{clientName:'💸 GASTO: '+concepto, monto:-abs, esCosto:true, esGasto:true}` — `esCosto` hace que el PDF y `generarReporteMensual` ya lo sumen solos como egreso y que "Cobrado mes" lo excluya; **`esGasto`** lo distingue de los costos de renovación de cuentas maestras. En `renderReportes` el card "Costos" del mes = `totalMonthlyCost() + gastosMes` (solo `esGasto`, para no duplicar renovaciones que ya representa `totalMonthlyCost`); bajo la barra de distribución sale la lista "💸 Gastos registrados este mes" con 🗑️ (`delPago`). La tarjeta "Ganancia" del dashboard NO incluye gastos (decisión del dueño de dejarla como está). El modal sigue la convención sin clic-fuera; concepto pasa por `sanTxt`.

**Top Clientes (Reportes):** bloque "🏆 Top Clientes" (IIFE inline en `renderReportes`, tras "Rentabilidad por Plataforma"). Agrupa `S.pagos` por `clientId` (excluye `esCosto`, sin `clientId`, y montos ≤ 0), top 5 por total histórico con medallas, barras `.bR/.bL/.bT/.bF` (barra lima con texto `#07130c`), nº de pagos y antigüedad (desde el primer pago). Nombre clicable → `irACliente(cid)` solo si el cliente aún existe en `S.clients`. El nombre mostrado es el último `clientName` de sus pagos (sobrevive a clientes borrados).

**PWA en GitHub Pages:** `sw.js` (service worker network-first, misma copia que `C:\puntoflix\public\sw.js`) y `404.html` están versionados en la raíz del repo para que `app.puntoflix.com` tenga caché offline de respaldo — antes el `register('sw.js')` daba 404 silencioso en ese dominio. Si se cambia el SW en Firebase, copiar también aquí.

**Recuperar contraseña (`resetPass`):** enlace "¿Olvidaste tu contraseña?" bajo el botón de login (`renderLogin`); usa `auth.sendPasswordResetEmail` con el correo del campo `#lu` y muestra el resultado (éxito verde / error rojo) en el div `#le`.

**Dashboard "Vencimientos Próximos" — filas unificadas:** un grupo con **varias cuentas** sale en **una fila** con badge `×N`, detalle "N cuentas: … · $total 🎁(si combo) · label", **💬** (mensaje combinado + `marcarEnviadoItems`) y **🛒 `openGrpRen(cId,phone,endDate)`** (modal para renovar todas o elegir cuáles). Las de **una sola** cuenta conservan lo original (💬 `genWAauto`, 🛒 `openRen`, 🗑️ `delSub`, avatar→`irASuscripcion`); los grupos multi **no llevan 🗑️** y el avatar/nombre van a `irACliente`. ⚠️ En la auditoría de botones (jul 2026) se quitaron los 🗑️ del dashboard "por seguridad" y el dueño pidió **restaurarlos** — prefiere el acceso rápido; NO volver a quitarlos. Scroll a `gruposUrg.length>8`. **Chevron ▾/▴** (`toggleUrg(gkey)`) despliega las cuentas individuales para **avisar** una sola (💬 `genWAauto`+`marcarEnviado` con el dueño real) o renovar una sola (🛒 `openRen`). Estado de despliegue en la variable de módulo **`_urgExpand`** (`{gkey:bool}`, NO se persiste); `toggleUrg` hace `renderCont()`.

**Modal de renovación de grupo (`openGrpRen`/`renderGrpRenMod`/`confirmarGrpRen`, estado `S.grpRenMod`):** para renovar varias cuentas de la misma persona (aunque sean 2 registros) eligiendo **cuáles**. `openGrpRen(cId,phone,end)` reúne los subs con **`_itemsGrupoUrg(cId,phoneDigits,end)`** (mismo criterio de agrupación) y preselecciona todas en `S.grpRenSel` (Set de `cId_sId`, no se persiste). El modal lista cada cuenta con checkbox + una sola fecha (botones +1/+3/+12 meses desde el vencimiento) + monto (autosuma de marcadas vía `_recalcGrpRenMonto`, sin re-render para no borrar la fecha escrita). `confirmarGrpRen` aplica la fecha a las marcadas (`endDate`, `startDate=hoy`, `pagado=true`) **a través de sus registros reales**, registra **un** pago bajo el primer dueño con el total, cierra el modal de inmediato, guarda en 2.º plano y llama `_ofrecerComprobante`. Enganchado en `renderApp` (dispatch), `closeAll` y el guard `editando` del sync en tiempo real.

**Caché de recordatorios (`_remCache`):** `getReminders()` cachea su resultado en `_remCache` para no recalcular los vencimientos varias veces dentro de un mismo render. **Ambos** `render()` y `renderCont()` lo ponen a `null` al entrar (se invalida por render), así el dashboard "Vencimientos Próximos" siempre usa datos frescos. OJO: la sincronización en tiempo real (`iniciarSyncTiempoReal`), `delSub` y la fusión de conflictos re-renderizan **solo** con `renderCont()`; si `renderCont()` no invalidara el caché, un cambio llegado desde otro dispositivo (p.ej. renovar en el celular) dejaría al cliente mostrándose como vencido/pendiente con la fecha vieja. No quitar el `_remCache=null` de `renderCont()`.

**Persistencia:** `sv()` guarda en Firestore + localStorage de emergencia. `ld()` carga al login con fallback local. Datos en `usuarios/{uid}` como JSON strings.

**Anti-pérdida de guardado (varias capas, no quitar):** (1) `_svCore` **devuelve un estado** `'ok'|'offline'|'error'|'sin-sesion'` y `sv()` lo propaga; los flujos críticos (p.ej. `submitCMod`) muestran un toast HONESTO según ese estado en vez de un "✅" fijo (antes mentía aunque el guardado fallara). (2) **Sesión ausente:** si en el momento de guardar `auth.currentUser` es null (refresco de token), `_svCore` ya NO aborta en silencio: respalda en localStorage bajo `_ultimoUid` y **reintenta** ~4 veces (1.2 s c/u) a que vuelva la sesión. `_guardarEmergencia(uid)` centraliza el snapshot local (antes inline). (3) **Online con transacción fallida:** se hace `ref.set` y se **espera** el resultado real (no fire-and-forget) para no reportar éxito falso; offline sí queda fire-and-forget + `'offline'`. (4) **Rescate fino al cargar** (`ld`): si la nube cargó pero el respaldo local es **fresco (≤2 h)** y tiene items por id que la nube perdió (clients/cuentas/pagos/proveedores), se re-inyectan y se re-sube con `sv()` (no revive lo que esté en papelera; ventana de 2 h para no resucitar borrados de otro dispositivo). (5) **`beforeunload`** avisa si `_syncEstado` es `'guardando'`/`'error'` (no cerrar con un cambio sin subir). (6) **Banner rojo fijo** (`#syncBanner`, z-index 100000, `_mostrarBannerSync`/`_ocultarBannerSync`/`_evaluarBannerSync` llamados desde `setSync`): aviso fuerte que el puntito discreto no da. Sale **de inmediato** en `'error'` y, si el `'offline'` dura **>12 s**, también; trae botón **🔄 Reintentar** (`reintentarSync` → `sv()`). Se quita solo al volver a `'ok'` y en `logout`. Animación `slideDownBanner`.

**Control de concurrencia (multi-dispositivo):** el documento se guarda **completo** (no por campos), así que sin protección "el último que escribe gana" y un dispositivo/pestaña con datos viejos puede pisar un cambio nuevo de otro (causa real de "lo cambié y se revirtió", p. ej. una transferencia). Fix: el doc lleva un campo `rev` (entero incremental); `aplicarDatosFirestore` registra `S._rev` al cargar/sincronizar; `sv()` escribe dentro de un `db.runTransaction` que lee la `rev` del servidor y, si avanzó respecto a `S._rev` (otro dispositivo guardó), **no pisa**: lanza conflicto. **En conflicto NO se descarta el cambio local** (antes sí → se perdían clientes recién creados): `_fusionarConflicto(serverData)` guarda el estado local, aplica lo del servidor (deja `S._rev` correcto) y **fusiona por id** las listas (`clients`, `pagos`, `cuentas`, `proveedores`, `papelera*`) — lo local gana en contenido y los items nuevos locales se conservan; los borrados de otro dispositivo se respetan filtrando lo que esté en la papelera. Luego `_svCore` **reintenta** el guardado (hasta 3 veces, parámetro `_intentos`); si tras 3 intentos sigue habiendo conflicto, fuerza un `ref.set` con lo fusionado (no perder el cambio). `_svCore` recibe `_intentos` y `sv()` lo encadena igual. Si la transacción falla por estar **sin conexión**, cae al `ref.set(payload)` directo (se encola offline) para no romper el modo offline; ahí sí puede haber clobber, pero offline es de un solo dispositivo. Tras escribir, `S._rev=payload.rev`. Docs legacy sin `rev` se tratan como `rev=0` y migran solos en el primer guardado. **Serialización:** `sv()` es un wrapper que encadena cada guardado al anterior (`_svCola=_svCola.then(_svCore,_svCore)`) y el cuerpo real está en `_svCore()`; así nunca corren dos guardados a la vez en el mismo dispositivo (evita que un guardado automático en 2.º plano y uno manual se pisen o disparen un falso conflicto). Todas las escrituras a Firestore pasan por aquí; no escribir al doc directo desde otro lado.

**Indicador de sincronización:** pill en la barra superior (`.tbr`, junto al `.darkToggle`) con punto de color + texto. Estados en `_syncEstado` ('ok'=🟢 Sincronizado, 'guardando'=🟡 pulsando, 'offline'=⚪ Sin conexión, 'error'=🔴). `renderSyncInd()` arma el HTML (se incluye en el topbar y se relee en cada render desde `_syncEstado`); `setSync(e)` actualiza el estado y reemplaza `#syncInd` por `outerHTML` sin re-render. `_svCore` llama `setSync('guardando')` al entrar y `'ok'`/`'offline'`/`'error'` según el desenlace. `init()` engancha `window` `online`/`offline`. En móvil el texto va en `.desktopOnly` (oculto), queda solo el punto. Animación `@keyframes syncPulse` (respeta `prefers-reduced-motion` por el bloque global de `.ov`/animaciones... el pulso es inline, no se desactiva solo; si molesta en reduce-motion, gatear). El listener en tiempo real (`iniciarSyncTiempoReal`) sigue igual; sus guards (`editando`, `<3s desde guardado propio`) que saltan la aplicación dejan `S._rev` viejo a propósito → fuerza el chequeo de conflicto en el siguiente guardado. El guard `editando` cubre `cMod/sMod/caMod/renMod/pagoMod/renCaMod/importMod/importCAMod/comboMod/provMod/bulkRenMod` (nota: los modales se dibujan **fuera** de `.cont`, así que aunque entrara un sync, `renderCont()` no borra un formulario abierto; el guard es por no cambiar los datos bajo el usuario).

**Datos principales:**
- `S.clients[]` → cada cliente tiene `subs[]` (plataforma, email, password, perfil, startDate, endDate, price, masterAccountId). `startDate` es campo editable ("Fecha de Inicio", default hoy) en el form de cliente nuevo (`#cstart`), en el modal de suscripción (`#ssd`) y se autollena con `nowDate()` en combos. Los botones rápidos +30d/+60d/+90d/1año calculan la Fecha de Fin **desde** la fecha de inicio. Se muestra en la tarjeta de cliente expandida y en el modal de renovación; ya estaba en export/import de Excel (columna "Inicio"). **Al renovar** (`confirmarRenovar`, la masiva `confirmarBulkRen` y el pago consolidado `confirmarPago`) la `startDate` se reescribe a `nowDate()` — la fecha de renovación pasa a ser la nueva fecha de inicio. **Cálculo del nuevo vencimiento (base):** los modales de renovación calculan la nueva fecha (default + botones +1mes/+3meses/+1año) **desde el vencimiento ANTERIOR**, no desde hoy — `baseRen=s.endDate||nowDate()` en `renderRenMod`, `baseRen=maxEnd||nowDate()` en el consolidado, `baseRenCa=ca.renewalDate||nowDate()` en `renderRenCaMod`. Antes era "si ya venció cuenta desde hoy"; se cambió porque el dueño **no corta el servicio: espera a que el cliente pague**, así no pierde los días esperados (si vence el 20 y paga el 23, +1 mes = 20 del mes siguiente, no 23). El `min` del input date = `baseRen`. El **masivo** (`confirmarBulkRen`) NO usa esta lógica porque aplica **una sola fecha a todas** las subs seleccionadas.

**Misma lógica en cuentas maestras (`S.cuentas[]`):** cada cuenta tiene `startDate` ("Fecha de Inicio", `#caStart` en `renderCAMod`/`submitCAMod`) además de `renewalDate` (su "fin"). Al renovar la cuenta (`confirmarRenCuenta`) la `startDate` pasa a `nowDate()`. Las listas (`renderCuentas` tabla y `renderCuentasAgrupadas`) muestran "Inicio: …" + "Renueva: …"; el modal `renderRenCaMod` muestra Inicio (con fallback "sin registrar → se pondrá hoy"). Export/import Excel de cuentas incluye la columna "Inicio". OJO: `saveCuenta` en edición hace `{id,...d}` (reemplaza el objeto entero), por eso `submitCAMod` **debe** incluir `startDate` en `d` o se perdería al editar.
- `S.cuentas[]` → cuentas maestras (las que paga el dueño). Cada cuenta puede tener `proveedorId` (a quién se la compró el dueño).
- `S.proveedores[]` → proveedores `{id,name,phone,notes}` (a quién le compras las cuentas). Sección propia en el sidebar (`🤝 Proveedores`, vista `renderProveedores`). Cada cuenta maestra se clasifica con `proveedorId` (selector `#caProv` en `renderCAMod`/`submitCAMod`). El form de **cliente nuevo** también trae selector de proveedor (`#cprov`) con opción "➕ Añadir nuevo" (`toggleNuevoProv` muestra `#nuevoProvBox` con `#cNuevoProv`/`#cNuevoProvTel`); `submitCMod` resuelve el proveedor (busca por nombre o lo crea) y lo asigna a la cuenta maestra auto-creada/vinculada de esa primera suscripción. Helpers: `proveedorDe(caId)`, `cuentasDeProveedor(provId)`, `verCuentasProveedor(provId)` (abre Cuentas filtrado por `S.fCuentaProv`, usa `'__none__'` para sin proveedor), `contactarProveedor(provId)` (WhatsApp general), `reportarProveedor(caId)` (WhatsApp al proveedor con plataforma+correo+clave+vence de la cuenta que falló — botón 🆘 en las filas/tarjetas de cuenta, solo si la cuenta tiene proveedor), `pedirRenovacionCuenta(caId)` (**pide al proveedor que renueve UNA cuenta maestra puntual** — botón 🔁 "Pedir renovación al proveedor" en los botones de acción de cada cuenta, en la vista agrupada `accRow` y en la tarjeta, solo si `ca.proveedorId`). Arma el mensaje con plataforma+correo+clave+vence de esa cuenta; **con teléfono** abre WhatsApp, **sin teléfono copia el mensaje** (`cp`) para pegarlo en Telegram/donde sea. ⚠️ El antiguo bloque "📅 Por renovar / Próx. pago" con botón "💬 Pedir renovación" **se quitó por completo de las tarjetas de proveedor**; `pedirRenovacionProveedor(provId)` **fue eliminada** (era código muerto). En `renderProveedores` las tarjetas se ordenan por fecha de pago más próxima (cuenta que vence antes primero, sin fecha al final). Modal `renderProvMod` (`openAddProv`/`openEditProv`/`submitProvMod`). **Papelera de proveedores** (`S.papeleraProveedores`): `delProv`/`delProvBulk` mueven a la papelera **conservando** el `proveedorId` en las cuentas (vínculo intacto para restaurar limpio); `restoreProv` regresa el proveedor a `S.proveedores`; `delProvForever`/`vaciarPapeleraProveedores` borran permanente y ahí sí limpian el `proveedorId` de sus cuentas. Vista `renderPapeleraProveedores` (toggle `S.showPapeleraProveedores`, botón "🗑️ Papelera (N)" siempre visible en la barra). Auto-purga 30 días en `_limpiarPapelera` (al purgar limpia `proveedorId`). Persistido en `sv()`/`aplicarDatosFirestore()`/respaldo local; reseteado en `setView`/`logout`/borrado total. Filtro "🤝 Proveedor" en la barra de Cuentas (`S.fCuentaProv`, aplicado en `cuentasOrdenadas`, `renderCuentasAgrupadas` y el contador `filtradas`). Persistido en `sv()`/`aplicarDatosFirestore()`/respaldo local. Nombre del proveedor visible en filas de cuenta (tabla, agrupada, tarjeta móvil). El respaldo Excel de cuentas exporta columnas "Proveedor" y "Tel Proveedor"; al importar, si la fila trae "Proveedor" lo busca por nombre (case-insensitive) o lo crea y enlaza `proveedorId`.
- `S.pagos[]` → historial de cobros
- `S.papelera[]` → clientes eliminados (recuperables)
**Selección múltiple (borrado en bloque):** clientes (`S.delMode`/`S.delSel`, `toggleDelMode`/`toggleDelSel`/`delClientsBulk`, checkbox en `itemRow` de la vista agrupada), cuentas maestras (`S.caDelMode`/`S.caDelSel`, `toggleCaDelMode`/`toggleCaDelSel`/`delCuentasBulk` → mueve a `papeleraCuentas`, checkbox en `accRow`) y proveedores (`S.provDelMode`/`S.provDelSel`, `toggleProvDelMode`/`toggleProvDelSel`/`delProvBulk` → borrado permanente, checkbox en la tarjeta). Botón "☑️ Seleccionar" en cada barra; en modo selección la barra muestra "🗑️ Eliminar N" + "Cancelar". Todos se resetean en `setView`.

- `S.papeleraCuentas[]` → cuentas maestras eliminadas (recuperables). `delCuenta` mueve la cuenta aquí (con `deletedAt`) y desvincula sus clientes (`masterAccountId=''`); `restoreCuenta` la regresa a `S.cuentas` (hay que re-vincular clientes con 🔗 Vincular); `delCuentaForever`/`vaciarPapeleraCuentas` borran permanente. Vista `renderPapeleraCuentas` (toggle `S.showPapeleraCuentas`, botón "🗑️ Papelera (N)" en la barra de Cuentas, solo si hay items). Auto-purga a 30 días en `_limpiarPapelera`. Persistido en `sv()`/`aplicarDatosFirestore()`/respaldo local; reseteado en `logout`/borrado total.
- `S.catalogo` → precios por plataforma

**Plataformas:** arrays globales `PLATS`, `PLATCOLORS`, `PI` (emojis), `PLAT_LIMITS`, `PLAT_URLS`. Plataformas custom en `S.customPlats[]`, se fusionan al cargar.

**Combos:** constante `COMBOS` (~línea 297: nombre, apps[], precio). `submitCombo()` crea/añade subs. `comboDe(subs)` detecta si un grupo de suscripciones forma un combo (compara plataformas con `platGroup`, sin importar orden); `totalSubs(subs)` devuelve `{total,combo}` usando el precio del combo si aplica, si no la suma. Usado en `genWACombinado`, `genWASeguimiento` y los totales de los modales de recordatorios/seguimiento — así un cliente con combo muestra el precio del combo, no la suma de los sueltos. Estado relevante: `S.comboClientMode` ('new'|'existing'), `S.comboEnd` (fecha persistida entre renders), `S.comboSel`, `S.comboClientId`.

**Modal de bienvenida** (`renderWelcomeMod`): detecta combo vía `S.welcomeComboSIds` (array de IDs). Si length > 1, usa `genWACombo()` en vez de `genWANuevaCuenta()`.

**WhatsApp:** siempre usar `openWA(phone, msg)` o `waLink(phone, msg)` para `<a href>`. No usar `wa.me` directamente.

**Recordatorios enviados (persistente):** `S.waEnviados` y `S.waSeguimiento` son objetos `{ 'cId_sId': endDate }` (no Sets), persistidos en Firestore. El marcador guarda la fecha de vencimiento, así que al renovar (cambia `endDate`) deja de coincidir y se puede volver a avisar. Marcar con `marcarEnviado/marcarEnviadoGrupo/marcarSeguimiento` (llaman `sv()`); chequear con `envEnviado(cId,sId,endDate)` / `segEnviado(...)`. Al cargar/sincronizar se limpian con `_prunMarcadores()` (descarta marcadores que no coinciden con una sub actual).

**Perfiles de cuenta:** `profInUse(caId)` cuenta perfiles ocupados + ajuste manual. `profDisponibles(caId)` = total - ocupados.

**Botones unificados (fila de cuenta maestra):** las 3 variantes (tabla desktop, agrupada `accRow`, tarjeta móvil) muestran solo **🛒 Renovar · 👁 Ver ocupantes · ✏️ Editar · ⋯**. El menú ⋯ lo arma **`_menuCuentaBtn(caId,uid)`** con: 📢 Notificar clientes, 🔁 Pedir renovación al proveedor y 🆘 Reportar (ambos solo si `ca.proveedorId`), y 🗑️ Eliminar (rojo, va a papelera). El `uid` debe ser único por variante (`caT_`/`caM_`/`caG_` + ca.id) porque la tabla desktop y las tarjetas móviles se renderizan juntas (CSS oculta una) y los ids `subMenu_*` chocarían. Reusa `toggleSubMenu`/`cerrarSubMenus`.

**Acceso a combos:** botón "🎁 Combo" en el topbar de Clientes (`renderTbr`, `desktopOnly`) y "🎁 Vender combo" en el menú "⋯ Más" de la barra de Clientes — antes `openCombo()` solo era alcanzable desde el modal de cliente nuevo (`crearComoCombo`), que sigue existiendo. El botón 🟣 "Cuenta completa" de la tarjeta móvil por suscripción se quitó y el dueño pidió **restaurarlo** (sigue ahí; no quitar).

**Botones unificados (fila de cliente):** la fila lleva 🛒 Renovar · 📨 Enviar datos · ✏️ Editar · ⋯. El botón 🚀 "Acceso rápido" se quitó de las filas (función `accesoRapido` sigue definida pero sin uso). En el menú ⋯, las dos opciones de aviso se llaman **"✅ Confirmar pago y nueva fecha"** (`waConfirmacionRenovacion`, tras cobrar) y **"⏰ Recordar que pague"** (`avisarRenovacionSmart`/`avisarVencimiento`, antes de vencer). Ambas las arma `_avisosMenu(cId,sId,endDate,recOnclick,recLabel)`: cada botón es de 2 líneas (título + subtítulo "Ya te pagó → mándale la fecha nueva" / "Aún no paga → recuérdale que renueve") con borde-izquierdo de color (verde=confirmar / ámbar=recordar). Según estado, la que toca va **arriba y resaltada** (fondo + borde): si `gst(endDate)!=='act'` (por vencer/vencida) → "Recordar que pague"; si activa/recién renovada → "Confirmar pago". `recOnclick` varía por menú (`avisarRenovacionSmart` en filas de cliente, `avisarVencimiento` en la vista agrupada). El botón 📨 abre `enviarDatosCliente(cId,sId)` → selector (modal `#chooserMod`) entre *Bienvenida* (`reenviarDatos`) o *Datos actualizados* (`enviarActualizacion`). El botón ✏️ abre `editarCliente(cId,sId)` → **pantalla combinada**: reusa `renderSMod` en modo edición con la bandera `S.sEditConCliente=true`, que añade arriba una sección "👤 Datos del cliente" (nombre/teléfono/estado/notas, ids `scn/scp/scstatus/scnotes`). `submitSMod` guarda ambos (cliente + suscripción) en un solo "Guardar". Si el cliente no tiene suscripción, `editarCliente` cae a `openEditC`. La bandera se resetea en `openAddS`/`openEditS`/`closeAll`/`saveSub`. **Propagación de credenciales:** al editar (no al crear) el correo/clave de una sub ligada a una cuenta maestra, `saveSub` actualiza también el registro de la cuenta maestra (`S.cuentas`) y todas las subs de cualquier cliente con el mismo `masterAccountId` (mismo comportamiento que editar desde "Cuentas"/`saveCuenta`); luego ofrece notificar por WhatsApp con `notificarClientesDeCuenta`.

**Borrado (cliente vs suscripción):** `delClient(id)` mueve el **cliente entero** (todas sus subs) a la papelera; `delSub(cId,sId)` borra **solo una** suscripción. Además, los 3 menús ⋯ de suscripción (fila single-sub de la tabla, vista agrupada `itemRow`, filas expandidas `renderSubRows`) incluyen al final **"🗑️ Eliminar cuenta maestra"** vía el helper **`_delCuentaMenuBtn(s)`** (definido junto a `delCuenta`): solo aparece si la sub tiene `masterAccountId` válido, muestra el correo de la cuenta como subtítulo y llama a `delCuenta(caId)` (va a `papeleraCuentas` y avisa de los clientes encima) — así se borra la cuenta maestra sin ir a la vista Cuentas. En menús `⋯` que cuelgan de una suscripción concreta (vista agrupada `renderClientesAgrupados`, filas expandidas `renderSubRows`, tarjeta móvil), si el cliente tiene **>1 sub** el menú ofrece *"Eliminar solo esta suscripción"* (`delSub`) y, como opción secundaria, *"Eliminar cliente completo"* (`delClient`); si tiene 1 sola, solo *"Eliminar cliente"*. No usar `delClient` desde un contexto de una sola suscripción de un cliente multi-sub (borraría todo).

**Cierre de modales:** los modales **NO** se cierran al hacer clic fuera del recuadro (se quitó el listener global en `render()` y los handlers inline `onclick="if(event.target===this){...}"` de cada `.ov`) — solo cierran con el botón **Cancelar**/**✕** (`closeAll()`) o con **Escape** (handler en el `keydown`, ~`if(e.key==='Escape')closeAll()`). Decisión para no perder datos de un formulario por un clic accidental. Al crear un modal nuevo, **no** volver a meter el patrón de clic-fuera. En la vista agrupada de Clientes (`renderClientesAgrupados`), la cabecera de cada grupo es la **cuenta maestra**: el nombre de plataforma (con flechita ↗) y el correo son clicables y llaman `irACuenta(caId)` con `event.stopPropagation()` para no disparar el colapsar/expandir del `.tb2`.

**Bienvenida — fecha de inicio:** la plantilla `bienvenida` y `genWACombo` muestran `📅 *Fecha de inicio:* {fechaInicio}` (de `s.startDate`) antes de "Válido hasta". `fechaInicio` está expuesta en `_plantData`, `PLANTILLAS_VARS.bienvenida` y `_sampleData`; se autooculta si la sub no tiene `startDate`.

**Menú ⋯ (dropdown):** `toggleSubMenu(sId,ev)` abre `#subMenu_<sId>` con `position:fixed` calculado contra la **ventana** (no contra `.cont`), para que no lo recorte el scroll del contenedor (`.cont` es `overflow-y:auto`). Decide abrir hacia arriba/abajo según el espacio y alinea el borde derecho con el botón ⋯. Se cierra con click fuera o scroll de `.cont` (`cerrarSubMenusUnaVez`). Al abrir, marca la tarjeta contenedora (`closest('.sc,.mCliCard,.caGRow,.tb2')`) con `.subMenuHostAbierto` (`transform:none!important`) para que el `:active{transform:scale}` de la tarjeta no re-ancle el menú fixed y el tap no caiga fuera del botón; `cerrarSubMenus` la quita. También sube el menú a `z-index:95` al abrirlo (los inline traen 50): por encima del FAB + (z 90) y la barra inferior móvil (z 50) que tapaban/robaban el toque de las últimas opciones, por debajo de los modales `.ov` (z 100). Si agregas menús nuevos, reusar esta función.

**Transferir suscripción:** `transferirCuenta(cId,sId)` abre un modal con las cuentas destino (misma plataforma, con cupo, ordenadas por más libres); `ejecutarTransferencia(cId,sId,nuevaCaId)` reasigna `masterAccountId`, copia correo/clave de la cuenta destino y sugiere un perfil libre (`sugerirPerfil`). La cuenta anterior libera el cupo sola (perfiles se cuentan por `masterAccountId`). Opción "🔄 Transferir a otra cuenta" en los 3 menús ⋯ (agrupado, tabla, por suscripción). Tras transferir ofrece avisar al cliente vía `enviarActualizacion`.

**Transferencia MASIVA (todos los clientes de una cuenta):** opción "🔄 Transferir TODOS los clientes (N) a otra cuenta" en el menú ⋯ de cuenta maestra (`_menuCuentaBtn`, solo si `nCli>0`). `transferirTodosCuenta(caId)` abre el modal `#transferAllMod` (DOM directo, sin clic-fuera) con las cuentas destino del mismo `platGroup` que tengan cupo para TODAS las subs (`profDisponibles>=mov.length`; si alguna sub es `cuentaCompleta`, exige destino con `profInUse===0`). `ejecutarTransferenciaMasiva(origenId,destinoId)` (con `confirm`) mueve cada sub con la MISMA regla que la individual (copia correo/clave, conserva perfil salvo vacío/choque → `sugerirPerfil`), guarda, y ofrece en un toast persistente (15s) avisar a todos vía `notificarClientesDeCuenta(destinoId)` (ese modal incluye también a los clientes que ya estaban en la cuenta destino — el dueño elige a quién enviar). La cuenta origen queda con el cupo libre; no se borra.

**Planes de varios meses / prorrateo (`mesesSub(s)`/`precioMensual(s)`):** la duración se DEDUCE de `startDate`→`endDate` (sin campos nuevos): ≤45 días = mensual (mes + gracia), si no `round(días/30.44)` (90d→3, 180d→6, 365d→12; sin fechas→1). `precioMensual = precioMostrar/mesesSub` y se usa SOLO en los cálculos "por mes": `cuentaIngresos` (→ `cuentaGanancia`, "+$X/mes" de cuentas), `oportunidadVenta`, "Rentabilidad por Plataforma" (Reportes) y el "$X/mes" del header de grupo en clientes agrupados. El **flujo de caja real NO se prorratea**: "Cobrado mes", pagos, `porCobrar`, `cliDeuda`, `cliTotal` siguen con el monto completo (la plata entra entera el día del cobro). UI: etiqueta azul "por N meses" junto al precio en la fila agrupada y la tarjeta móvil cuando `mesesSub>1`. Motivo: vender Spotify 3 meses a $8 inflaba el ingreso/mes de la cuenta como si fueran $8 mensuales.

**Precios:** `precioMostrar(s)` = precio propio del cliente (`s.price`) y, si no tiene, el del catálogo (`getCatalogoPrecio`). El precio individual **manda** sobre el catálogo, así que cambiar el catálogo NO toca clientes existentes. Para empujar precios del catálogo a clientes ya creados: botón "🔄 Aplicar a clientes existentes" en Ajustes → Catálogo (`aplicarCatalogoAClientes`/`ejecutarAplicarCatalogo`, con preview y selección por plataforma).

**Búsqueda:** Clientes → `filtered()` (estado `S.search`) busca por nombre, teléfono, plataforma, correo (`accountEmail`), nombre de perfil (`profile`) y PIN. `filtered()` deja pasar al **cliente entero** si cualquier sub coincide; en la **vista agrupada** eso arrastraba las otras cuentas del cliente (buscar un correo de cuenta mostraba grupos "no relacionados"), así que `renderClientesAgrupados` aplica además **`subMatchBusqueda(c,s)`** a nivel de sub (definida justo antes): si lo buscado coincide con nombre/teléfono del cliente → todas sus subs; si solo coincide con datos de sub (correo/plataforma/perfil/PIN) → solo las subs que coinciden. Se salta cuando hay foco (`focusActivo`). Al añadir campos buscables a `filtered()`, añadirlos también aquí. El filtro "⏰ Vencimiento" incluye la opción **"— Sin fecha"** (`fStat='unk'`, estado que `cliSt` da a clientes sin subs o sin fechas). Como la vista agrupada recorre **suscripciones**, los clientes con CERO subs no caían en ningún grupo y quedaban invisibles (el contador los contaba pero salía "Sin resultados"): `renderClientesAgrupados` los junta en `sinSubs[]` y los pinta en una sección propia **"👤 Sin suscripción"** (colapsable `grp_nosub`, con ➕ Agregar suscripción / 📋 / ✏️ `editarCliente` que cae a `openEditC` / 🗑️ `delClient`, y checkbox de `delMode`). Se omite con foco activo. Cuentas → `cuentaMatch(ca,q)` busca por correo/plataforma de la cuenta + `cuentaMatchCliente` (cliente vinculado por nombre, teléfono, correo, perfil, PIN). Al añadir campos buscables, tocar ambos.

**Ir a un cliente/cuenta:** `irACliente(id)` abre Clientes filtrado en ese cliente; `irACuenta(caId)` abre Cuentas filtrado en esa cuenta. `irASuscripcion(cId,sId)` abre Clientes y muestra/resalta **solo esa suscripción** (usado desde el dashboard "Vencimientos Próximos", donde cada fila es una sub concreta). Para mostrar solo la sub elegida (no todas las del cliente) usa `S.focusSub=sId`: `renderSubRows` y la tarjeta móvil filtran a esa sub y añaden un botón "▾ Ver las otras N cuenta(s)" (`verTodasLasSubs(cId)` → `S.focusSub=null`). El foco se limpia solo al ir al cliente completo (`irACliente`) o al expandir/colapsar manual (`toggleColapsar`). Las filas/bloques de suscripción llevan `data-sub="<sId>"` (filas expandidas `renderSubRows`, fila de cliente single-sub, vista agrupada, tarjeta móvil) además de `data-cli`; el resaltado de sub usa `S.highlightSub` + clase CSS `.cliHL` (con fallback a `data-cli`). El resaltado a nivel cliente sigue usando `S.highlightClient`/`S.highlightCuenta`. Ya enganchados en los paneles del dashboard (Vencimientos Próximos → cliente, Tus Cuentas por Renovar → cuenta). Reusar para navegar desde cualquier vista.

**Plantillas de mensaje:** los textos de WhatsApp se generan vía plantillas editables. Defaults en `PLANTILLAS_DEF` (claves: `porVencer`, `vencido`, `seguimiento`, `comprobante`, `bienvenida`); overrides del usuario en `S.plantillas`. Los recordatorios `porVencer`/`vencido` incluyen líneas `📧 Correo:`/`👤 Perfil:` (se autoocultan si vacías); el mensaje combinado `genWACombinado` muestra correo y perfil por cuenta. `aplicarPlantilla(key,data)` + `_renderTpl()` reemplazan `{variables}` y quitan líneas de dato vacías. La plantilla `bienvenida` incluye `📅 *Fecha de inicio:* {fechaInicio}` además de `📅 *Válido hasta:* {fecha}`; `fechaInicio` sale de `s.startDate` (variable expuesta en `_plantData`, `PLANTILLAS_VARS.bienvenida` y `_sampleData`, se autooculta si no hay fecha). `genWACombo` también imprime la línea de inicio (`subs[0].startDate`). Los generadores single-sub (`genWAauto`, `genWARenovar`, `genWASeguimiento`, `genWAConfirmacion`, `genWANuevaCuenta`) pasan por ahí; los **combinados** (`genWACombinado`, `genWACombo`, `genWAMsg` multi) siguen siendo automáticos. UI en Ajustes (`PLANTILLAS_INFO`). **Datos de cobro:** `S.datosPago` (string libre, persistido) se expone como variable `{datosPago}` en `_plantData` y está en las plantillas `porVencer`/`vencido`/`seguimiento` como línea `💳 Paga aquí: {datosPago}` (se autooculta si está vacío). Se edita en Ajustes → "💳 Tus datos de cobro" (`saveDatosPago`, textarea `#sDatosPago`). Recordar incluir `datosPago` en `sv()` y `aplicarDatosFirestore()`.

**Auto-asignar cuenta:** al crear suscripción se elige sola la cuenta maestra con **más perfiles libres** (desempata por menor costo), tanto en el form de cliente nuevo (`updateCuentaDisponible`) como en el de suscripción (`autoAsignarCuenta`); red de seguridad en `saveSub`. No vende sobre cuentas llenas o "completas".

**Reporte mensual PDF:** `descargarReporteMensualPDF(ym)` genera un PDF (jsPDF cargado on-demand vía `loadJsPDF()`, CDN, igual patrón que `loadXLSX`) con encabezado PUNTOFLIX, 3 tarjetas (Ingresos/Costos/Ganancia neta de `generarReporteMensual(ym)`), tabla "Ventas por plataforma", "Detalle de cobros" y "Costos/egresos", con paginación. Helper de tablas: `_pdfTabla(doc,x,y,width,cols,rows,H,M)` (cols = `[label, fracciónAncho, align?]`). jsPDF usa helvetica estándar → **no** meter emojis en el texto del PDF (no renderizan); acentos sí. Incluye sección "Comparación con [mes anterior]" (`ymAnterior(ym)` + `generarReporteMensual` del mes previo, variación % por métrica). Botones: en el modal `renderReporteMensualMod` y en la vista Reportes (este mes / mes anterior). Los datos salen de `S.pagos` (ingresos vs `esCosto`/monto negativo). **Descarga semi-automática:** `checkReporteMensual` (corre al abrir la app en un mes nuevo, guard por `localStorage` `pf_report_<uid>` → una sola vez por mes) muestra el modal y además dispara `descargarReporteMensualPDF(ym)` con un `setTimeout` de 800ms. No es 100% automático: depende de que el usuario abra la app y el navegador puede pedir confirmar la descarga.

**Automatizaciones existentes** (no reimplementar; corren al abrir la app o tras una acción): auto-respaldo, auto-suspender vencidos, resumen del día (`mostrarResumenDia`), recordatorios semi-automáticos (`enviarTodosDia`), **seguimiento de cobros** (`getSeguimiento`/`renderSeguirMod`), **reporte mensual** (`checkReporteMensual`), **confirmación de pago** tras renovar/cobrar (`enviarConfirmacionPago`, configurable `S.confirmacionPago`), **aviso masivo de cambio de clave** (`notificarClientesDeCuenta`, botón 📢). Flags persistidos: `S.seguimiento/seguimientoDias`, `S.reporteMensual`, `S.confirmacionPago`, `S.plantillas` — recordar añadirlos a `sv()` y `aplicarDatosFirestore()`.

**Puntualidad de pago (`puntualidadCliente(c)`/`puntBadge(c)`, junto a `cliDeuda`):** score automático 🟢 Puntual (avg≤1 día) / 🟡 Se atrasa (≤5) / 🔴 Moroso (>5), calculado SOLO del historial — sin campos de UI nuevos (respeta la decisión de no tocar el flujo de cobro). Fuentes de atraso: (1) pagos con **`venciaEl`** (el vencimiento ANTERIOR, capturado antes de sobreescribir `s.endDate` en los 4 flujos: `confirmarRenovar`, `confirmarPago`, `confirmarGrpRen`, `confirmarBulkRen` — a este último también se le añadió `endDate` al pago, antes no lo llevaba); (2) heurística legacy: pares consecutivos de pagos de la MISMA `platform` → días entre `prev.endDate` y `p.fecha` (válidos −60..+120); (3) subs vencidas HOY (`-dl`, tope 120). Mínimo 2 datos para calificar, si no `null`/sin badge. Cache de módulo **`_puntCache`** anulado junto a `_remCache` en `render()` y `renderCont()` (no quitar). Badge visible junto al nombre en: fila de tabla, fila agrupada (`itemRow`), tarjeta móvil, y en el modal de **seguimiento de cobros** (que además ordena los no-enviados por peor promedio primero); resumen completo en el historial (`renderHistorialMod`). `venciaEl` viaja dentro de `S.pagos` (nada nuevo en `sv()`/`aplicarDatosFirestore`).

**Deuda / badge de clientes:** `cliDeuda(c)` filtra por `gst(s.endDate)==='ven'` (NO por `s.pagado` — el campo `pagado` en subs existe pero no se resetea automáticamente; la deuda real se mide por vencimiento). El badge desaparece al renovar (nueva fecha → `gst` deja de ser 'ven').

**Coherencia de métricas (barrido jul 2026):** `cliTotal`/`cliDeuda` usan **`precioMostrar`** (propio o catálogo), igual que `porCobrar` — antes usaban `s.price` crudo y un cliente sin precio propio mostraba $0 de deuda. `byPlat()` y "Rentabilidad por Plataforma" **excluyen cancelados y agrupan por `platGroup`** (antes el gráfico contaba cancelados y una cuenta "Paramount Mundial" no cruzaba con subs "Paramount+"). Los badges del sidebar (`expN/venN/deuN` en `renderApp`) **excluyen cancelados** (como el dashboard). Los precios mostrados en la fila single del dashboard y la tarjeta móvil usan `precioMostrar` (antes `s.price` crudo → "$0.00" con precio de catálogo). `dataBytes()` incluye proveedores y las 3 papeleras. Decisiones del dueño: (a) la tarjeta **"Ganancia" del dashboard se queda como está** (mesRev − `totalMonthlyCost`, SIN gastos manuales; Reportes sí los resta — diferencia aceptada, no "corregir"); (b) los **Suspendidos NO cuentan en "Por Cobrar"** — `stats()` calcula `exp/ven/deuda/porCobrar` sobre `soloAct` (solo `Activo`), igual que los badges del sidebar (`_soloAct` en `renderApp`), para cuadrar con recordatorios/seguimiento que ya eran solo-Activos. `st.act`/`total` siguen sobre no-cancelados.

**Cierre de modal de renovación + aviso al cliente:** `confirmarRenovar` y `confirmarPago` cierran su modal (`render()`) **de inmediato y lanzan `sv()` en 2.º plano (sin `await`)** — antes hacían `await sv()` ANTES de cerrar, así que en el celular con guardado lento (reintentos de token/offline, hasta ~5s) el modal "se quedaba ahí" como trabado. **Mismo fix aplicado (jul 2026) a `submitCMod` (crear cliente) y `submitCombo`:** el form de cliente nuevo se quedaba congelado sin avanzar al modal de bienvenida esperando `await sv()` en 4G lento; ahora cierran + abren la bienvenida al instante y el toast honesto del guardado (ok/offline/error) llega vía `sv().then(res=>…)` cuando el guardado termina. **Barrido completo (jul 2026): se eliminó TODO `await sv()` que bloqueara la UI** — patrón nuevo: `render()/renderCont()` + `toast` primero, `sv()` al final sin await (la cola `_svCola` serializa igual y el banner rojo/indicador cubren fallos). Cubiertos: `saveSub` (modal suscripción/edición combinada), `saveCuenta` (modal cuenta maestra), `submitProvMod`, `confirmarRenCuenta`, `confirmarBulkRen`, `confirmImport`/`confirmImportCA` (su único guardado es el `sv()` de `vincularSuscripcionesACuentas`, ya sin await; a `confirmImportCA` se le añadió el `render()` que le faltaba — el modal no se cerraba visualmente), transferencias individual/masiva, papeleras de cuentas/proveedores, `toggleCuentaCompleta`, unir duplicados, aplicar catálogo, desvincular ocupante, archivar pagos y todos los toggles/guardados de Ajustes. ÚNICA excepción con `await sv()`: `runAutoBackup` (corre solo en 2.º plano, no bloquea). Si un flujo nuevo guarda tras una acción del usuario, seguir este patrón (nunca `await sv()` antes de actualizar la UI). El respaldo local anti-pérdida y el indicador/banner de sync siguen cubriendo los fallos de guardado. Tras cerrar, ambos llaman a **`_ofrecerComprobante(pagoId,msgToast)`**: un **modal centrado y PERSISTENTE** (DOM directo `#renOkMod`, patrón de `chooserMod`, no se autodestruye — solo cierra con ✕/botones) que ofrece "📲 Enviar comprobante por WhatsApp" (`enviarConfirmacionPago`). Reemplazó al toast de esquina que se borraba a los 9s y en móvil no se alcanzaba a ver. Si no hay teléfono o `S.confirmacionPago===false`, cae a un `toast`.

**Modal de renovación consolidada (`renderPagoMod`/`confirmarPago`, "🔄 Renovar y Cobrar"):** se abre con el botón 🛒 de un cliente con **>1 suscripción** (`renovarClienteRapido`→`openPago`). Antes solo registraba el pago sin tocar fechas (el cliente seguía vencido y con badge de deuda); ahora **renueva**: pide una nueva fecha de vencimiento (con botones +1/+3/+12 meses sobre el vencimiento más lejano), la aplica a las cuentas **vencidas** (o a todas si ninguna venció), pone `startDate=nowDate()` y registra el pago con `endDate=nuevaFecha`. Si `#pFecha` queda vacío hace fallback al comportamiento viejo (solo registrar pago). `enviarConfirmacionPago` usa el `endDate` del pago, así el comprobante muestra la fecha nueva.

**Funciones eliminadas (código muerto):** `accesoRapido`, `abrirPestanaNueva`, `openEditS`, `genWARenovar`, `waOne`, `pedirRenovacionProveedor` — todas sin referencias de entrada, eliminadas. Variable `S.notifQueue` también eliminada. CSS muerto eliminado: `.bPag`, `.bDeu`, `.bSusp`, `.bCanc`, `.bdgY`, `.bdgB`.

**Ajustes en pestañas:** `renderSettings` agrupa sus ~13 secciones en 4 pestañas (`S.settingsTab`: `negocio`|`mensajes`|`auto`|`datos`, default `negocio`) — barra de botones arriba que cambian con `renderCont()`. Cada grupo de secciones va envuelto en `${T==='<tab>'?\`…\`:''}`. Reparto: **negocio** (nombre, datos de cobro, WhatsApp, días recordatorio, catálogo, agrupar plataformas), **mensajes** (plantillas), **auto** (automatizaciones, auto-respaldo), **datos** (respaldo, seguridad, mantenimiento, resumen/zona de peligro). Al añadir una sección nueva, mete su HTML dentro del gate de la pestaña que corresponda.

**Tema (estilo Bayzon, lima + navy oscuro):** acento **verde lima** `--acc:#a3e635`/`--acc2:#bef264` (antes esmeralda #22c55e); hover lima oscuro `#84cc16`. Tintes de acento = `rgba(163,230,53,…)`. **Contraste:** como el lima es muy claro, el texto sobre fondo lima es **oscuro** `#07130c` (botones `.bp`, FAB, nav activo `.ni.on`, barra `.bF`, ícono PWA). Para **texto en color acento** existe `--accTx` que se adapta al tema (claro `#4d7c0f` / oscuro `#a3e635`); usar `color:var(--accTx)` (NO `var(--acc)`) en textos para que se lean en modo claro. Radios `--radius:14px`/`--radius2:10px`. **Fondo navy oscuro** (dark): `--bg:#0d1424`, `--surf:#141c30` (tarjetas apenas se despegan del fondo), sidebar = `--bg` (fundido, separado por borde). **Tipografía:** `Plus Jakarta Sans` (antes Inter). Tamaños subidos a escala Bayzon: título de página `.tbr h2` 20px/800, nav `.ni` 14px/600, `.sc .lb` 12.5px y en **blanco** (`--text`), `.sc .vl` 30px, `.twTitle` 15px. Nav inactivo en blanco (`--text`); títulos blancos, subtítulos en gris (`--text2`). **Oscuro por defecto:** `init` activa dark salvo `localStorage pf_dark==='0'`; `toggleDark` guarda `'1'`/`'0'` **y actualiza el ícono del botón en el acto** (sin render); estado `dark:true`. El ícono del toggle refleja el modo actual: 🌙 en oscuro, ☀️ en claro. El **rojo de Netflix** (`PLATCOLORS.Netflix:'#e50914'`) se conserva a propósito — no convertir. Gradientes/íconos PWA/splash/logo en lima. **Animaciones (estilo Bayzon):** entrada de modales (`modIn`/`ovIn`), transición de vista (`.cont.viewIn`, añadida en `setView`), conteo animado de números del dashboard (`animarNumeros`, llamado en `render` cuando `view==='dashboard'`), entrada escalonada de tarjetas `.sg .sc`, hover con sombra de color en `.sc`, glow en nav activo, deslizamiento `.ni:hover`/giro `.fabBtn:hover` (`@media(hover:hover)`), y **aparición en cascada al desplegar una caja** (`.revWrap` + `@keyframes revealIn`): `toggleColapsar` marca `S._reveal=id` al abrir y solo ese grupo envuelve sus filas en `.revWrap` (se limpia tras `renderCont` para no repetir al filtrar). Todo respeta `prefers-reduced-motion`.

**Animación de clic (feedback de presión):** reglas CSS `:active{transform:scale(...)}` junto a los `@keyframes` del `<style>`. Nivel 1 (controles, scale .93): `.btn/.ib/.iconActionBtn/.cpB/.expBtn/.subMenuItem/.ni/.fabBtn/.darkToggle/.sbToggle/.pT/.fs`. Nivel 2 (tarjetas/cabeceras, scale .985): `.sc/.mCliCard/.caGRow/.tb2[onclick]` (a `.caGRow`/`.tb2[onclick]` se les añadió `transition:transform .12s`). Respeta `prefers-reduced-motion`. No usar `[onclick]:active` global (afectaría overlay/modales). ⚠️ **Bug corregido (jul 2026):** el scale del Nivel 2 en la tarjeta contenedora convertía a la tarjeta en containing block del menú ⋯ (`position:fixed`) al tocar una opción → el menú saltaba y el tap nunca llegaba al botón (las 4 opciones del menú de cuenta "no funcionaban" en móvil/vista agrupada). Fix: clase `.subMenuHostAbierto{transform:none!important}` que `toggleSubMenu` pone en el `closest('.sc,.mCliCard,.caGRow,.tb2')` al abrir y `cerrarSubMenus` quita. Si se añade transform/:active a otro contenedor de menús ⋯, incluirlo en ese `closest`.

## Validar tras editar

El archivo es un solo HTML con JS inline; un literal/llave mal cerrado rompe toda la app en silencio. Verificar la sintaxis sin navegador (el script principal es el **último** bloque `<script>`):

```bash
node -e "const fs=require('fs');const h=fs.readFileSync('puntoflix_firebase.html','utf8');const p=h.split(/<script[^>]*>/);const last=p[p.length-1].split('</script>')[0];try{new (require('vm').Script)(last);console.log('JS OK '+last.length);}catch(e){console.log('ERR '+e.message);}"
```
